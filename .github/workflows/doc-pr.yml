name: Doc-PR

on:
  pull_request:
    branches:
      - master

jobs:
  generate-document:

    runs-on: macos-26

    steps:
    - uses: actions/checkout@v6

    - uses: ruby/setup-ruby@v1
      with:
        bundler-cache: true

    - name: Generate document
      run: bundle exec jazzy
